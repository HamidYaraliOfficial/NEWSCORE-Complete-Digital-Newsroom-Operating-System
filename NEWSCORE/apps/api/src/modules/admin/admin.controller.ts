import { Body, Controller, Get, Param, Patch, UseGuards } from '@nestjs/common'; import { AdminService } from './admin.service'; import { AuthGuard } from '../../common/auth.guard'; import { Roles } from '../../common/roles.guard';
@Controller('admin') @UseGuards(AuthGuard) export class AdminController {constructor(private readonly s:AdminService){} @Get('overview') overview(){return this.s.overview();} @Get('users') users(){return this.s.users();} @Get('roles') roles(){return this.s.roles();} @Get('flags') flags(){return this.s.flags();} @Patch('flags/:key') flag(@Param('key')key:string,@Body()b:any){return this.s.setFlag(key,Boolean(b.enabled));} @Get('audit') audit(){return this.s.audits();}}
