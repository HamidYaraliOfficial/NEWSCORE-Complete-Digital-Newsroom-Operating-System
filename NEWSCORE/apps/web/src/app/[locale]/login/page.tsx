'use client';
import {useState} from 'react'; import {useRouter,useParams} from 'next/navigation'; import {Button,Card,Input} from '@/components/ui'; import {login} from '@/lib/api';
export default function Login(){const r=useRouter();const p=useParams<{locale:string}>();const [email,setEmail]=useState('admin@newscore.local');const [password,setPassword]=useState('ChangeMe!123456');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{await login(email,password);r.push(`/${p.locale}/dashboard`)}catch(err:any){setError(err.message)}finally{setLoading(false)}}return <main className="grid min-h-[calc(100vh-64px)] place-items-center px-4"><Card className="w-full max-w-md p-6"><div className="text-2xl font-black">NEWSCORE</div><p className="mt-1 text-sm text-slate-500">Editorial workspace</p><form className="mt-6 space-y-3" onSubmit={submit}><Input value={email} onChange={e=>setEmail(e.target.value)} type="email"/><Input value={password} onChange={e=>setPassword(e.target.value)} type="password"/><Button className="w-full" disabled={loading}>{loading?'…':'Sign in'}</Button>{error&&<div className="rounded-xl bg-red-500/10 p-3 text-xs text-red-700">{error}</div>}</form></Card></main>}
